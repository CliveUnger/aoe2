# Python virtualenv
venv/

# Editable mgz clone — separate git repo (our hard fork CliveUnger/aoc-mgz, on master).
# Reproduce per README; not vendored here.
aoc-mgz/

# Python caches
__pycache__/
*.py[cod]

# vill_ledger.py's extract_full JSON cache
data/extracts/

# replaylib.py's pickled-parse cache (auto-invalidates on replay/parser change)
data/match_cache/

# macOS
.DS_Store
.venv/
